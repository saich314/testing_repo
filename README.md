# stacked_pr playground

A tiny calculator used to learn **stacked PRs**.

- `calc.py` — the code
- `test_calc.py` — run with `python3 -m unittest -v`
- `STACKED_PRS.md` — the guide (added later in the stack demo)
